module.exports = {
    done: '✅ | ',      // Success
    fail: '❌ | ',      // User error, e.g missing argument
    error: '⚠️ | ',     // Non-user error, e.g a bug or database issue
    pending: '⏱️ | '    // If you have to wait for something (meant for embeds which will be editted)
}